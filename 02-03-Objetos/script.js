var person = {
    name: 'Reydner',
    age: 27,
    sex: 'male'
} //objeto

person.age  = 28 // reatribuição da propriedade no objeto

console.log(person)
console.log(person.name) // há a possibilidade de acessar some uma propriedade
console.log(person.age) // dentro do objeto conforme esta nos exemplos 
console.log(person.sex)